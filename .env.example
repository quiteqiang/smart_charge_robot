# Copy to .env to override defaults used by docker-compose.yml

# DDS domain isolation (change if you run multiple robot systems)
ROS_DOMAIN_ID=42

# X display for RViz2 GUI forwarding (empty = headless operation)
DISPLAY=
